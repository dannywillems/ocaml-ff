name: OCaml CI
on:
  pull_request:
    types: [assigned, opened, synchronize, reopened]
    branches:
      - master
  push:
    branches:
      - master
  merge_group:
    types: [checks_requested]

jobs:
  check-format:
    name: Check formatting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: "5.3"
      - name: Install ocamlformat
        run: opam install ocamlformat.0.29.0
      - name: Check formatting
        run: opam exec -- make check-format

  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
        ocaml-compiler:
          - "5.0"
          - "5.1"
          - "5.2"
          - "5.3"
          - "5.4"
    steps:
      - uses: actions/checkout@v6
      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: ${{ matrix.ocaml-compiler }}
      - name: Install dependencies
        run: opam exec -- make setup
      - name: Build
        run: opam exec -- make build

  test:
    name: Test
    needs: build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
        ocaml-compiler:
          - "5.0"
          - "5.1"
          - "5.2"
          - "5.3"
          - "5.4"
    steps:
      - uses: actions/checkout@v6
      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: ${{ matrix.ocaml-compiler }}
      - name: Install dependencies
        run: opam exec -- make setup
      - name: Run tests (native + JS)
        run: opam exec -- make test

  build-doc:
    name: Build documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: "5.4"
      - name: Install dependencies
        run: opam exec -- make setup
      - name: Build documentation
        run: opam exec -- make doc
